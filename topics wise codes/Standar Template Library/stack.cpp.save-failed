#include<bits/stdc++.h>
using namespace std;

int main()
{
        stack <int> st;
        st.push(100);
        st.push(200);

        while( !st.empty() )
        {
            cout<<st.top<<endl;
            st.pop();
        }
}
