#include<bits/stdc++.h>
using namespace std;
struct data{
    int w,q,i;
};
vector<data>v;

data compare(data a, data b)
{
    if()
}
int main()
{
    int n;
    data temp;
    scanf("%d",&n);

    for(i=0; i<n; i++)
    {
        scanf("%d %d",&x, &y);
        temp.w=x;
        temp.q=y;
        temp.i=i;
        v.push_back(temp);
    }

    sort(v.begin(), v.end(),compare);
}
