#include<bits/stdc++.h>
using namespace std;
int n;
int b[50009];
struct data
{
    int first,second;
};
data p[50009];
int main()
{

    int q,i,j,point,num1, num2;

    scanf("%d %d",&n,&q);

    for(i=0; i<n; i++)
    {
        scanf("%d %d",&p[i].second,&p[i].first);

        a[i]=p[i].second;
    }


    sort(a,a+n);
    sort(p,p+n);





}
